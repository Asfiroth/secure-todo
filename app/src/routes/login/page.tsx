import { Suspense } from 'react';
import { View } from '~/routes/login/Components/View';

const Login = () => {
  return (
    <Suspense>
      <View />
    </Suspense>
  );
};

export default Login;
